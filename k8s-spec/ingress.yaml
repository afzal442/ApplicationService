apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: res-ingress
spec:
  ingressClassName: nginx
  rules:
    - host: myres-svc.172.105.45.105.nip.io
      http:
        paths:
          - pathType: Prefix
            path: "/v1"     
            backend:
              service:
                name: restuarant-service
                port:
                  number: 80
